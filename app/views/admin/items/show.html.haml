.panel.panel-default
  .panel-body
    %h1= @item.name
    %p
      %span.label.label-default= @item.category.name
    = raw @item.description
    %br
    = link_to "Редактировать", edit_admin_item_path(@item), class: "btn btn-info btn-sm"

