= simple_form_for([:admin, @category]) do |f|
  = f.error_notification

  .form-group
    = f.input :name, input_html: {class: "form-control"}, placeholder: "Введите заголовок", label: "Заголовок"

  .form-actions
    = f.submit "Применить", class: "btn btn-success"
